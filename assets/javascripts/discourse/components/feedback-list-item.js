import Component from "@glimmer/component";

export default class FeedbackListItem extends Component {
  // Component for displaying individual feedback items
  // Receives @feedback prop with user rating data
}
